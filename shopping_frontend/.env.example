# Frontend environment variables
# Copy to .env and adjust as needed. Do NOT commit real secrets.
# When not set, the app defaults to http://localhost:3001
# Optional: set a full backend URL (overrides same-origin default)
# Example: http://localhost:3001 or https://your-backend.example.com
REACT_APP_API_BASE_URL=
